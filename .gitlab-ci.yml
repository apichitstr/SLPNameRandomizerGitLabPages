pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp -r index.html unit-converter.html hub.css styles.css converter.js app.js A.txt B.txt C.txt public/
  artifacts:
    paths:
      - public
  only:
    - main
    - master
